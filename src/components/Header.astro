<header class="pointer-events-none">
    <nav class="flex justify-between">
        <div class="flex-column pointer-events-auto">
            <div class="text-old font-extralight">ANAS</div>
            <div>
                <h1>Creative front<span class="max-md:hidden font-extralight">end</span> dev<span class="max-xl:hidden">eloper</span></h1>
                <!-- <div><span class="max-md:hidden">& </span>Artist by nature</div> -->
                 <div>Portfolio soon</div>
            </div>
            <div class="pointer-events-auto flex gap-1.5">
                <div id="theme-toggle" />
            </div>
        </div>

        <div class="pointer-events-auto">
            <a class="underline underline-offset-3 text-right" href="mailto:anas.frontdev@gmail.com">Let's work together</a>
        </div>
    </nav>
</header>

<script>
    document.addEventListener("DOMContentLoaded", function () {
      const toggleButton = document.getElementById("theme-toggle");
      const htmlElement = document.documentElement;

      const handleLightMode = () => {
        const currentTheme = htmlElement.getAttribute("data-theme");
        const newTheme = currentTheme === "light" ? "dark" : "light";
  
        htmlElement.setAttribute("data-theme", newTheme);
      }
  
      toggleButton?.addEventListener("click", handleLightMode);
    });
  </script>